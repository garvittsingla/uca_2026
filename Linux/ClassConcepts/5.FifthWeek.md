# Bits and bytes

How can we store a suite and number of the playing cards in the most optimal way 

1. Best answer to use a single byte or a char to do it , not using a pair and something like that , it will take a lot of bytes and space in the memory
->implement this in a programming language in c
-> bitwise and using only ~ and !